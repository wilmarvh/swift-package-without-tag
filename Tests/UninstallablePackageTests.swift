import XCTest
    @testable import UninstallablePackage

    final class UninstallablePackageTests: XCTestCase {
        func testExample() {
            XCTAssertEqual(UninstallablePackage().text, "Hello, World!")
        }
    }